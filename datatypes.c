#include <stdio.h>
#include <stdlib.h>

int main(void){
    int n ;
    float nf;
    char na;

    printf("Intenger size = %lu bytes\n", sizeof(n));
    printf("Float size = %lu bytes\n", sizeof(nf));
    printf("caractere sizr = %lu bytes\n", sizeof(na));
    return 0;
}
