#include <stdio.h>
#include <stdlib.h>

int main(void){
    int length = 4;
    int width = 6;
    int area;

    area = length * width;

    printf("Length is: %d \n", length);
    printf("Width is: %d \n", width);
    printf("Area of the rectangle is: %d \n", area);

    return 0;
}
